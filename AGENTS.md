# 岁时记 - 需求拆解文档

## 产品概述

- **产品类型**: 古风悠闲经营游戏（单页互动应用）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **游戏类型**: 休闲经营 / 收集养成
- **目标用户**: 喜爱中国传统文化、二十四节气、古风美学的休闲玩家
- **核心玩法**: 玩家以隐士身份随二十四节气轮转经营庭院，参与节气活动，收集物候风物，邂逅山中访客，享受田园之趣
- **界面语言**: 中文
- **主题偏好**: user_specified（中国古风水墨淡彩，淡墨灰、米白底色 + 四季代表色：春嫩绿/夏青碧/秋金黄/冬银白）
- **导航模式**: 无导航（单页应用，多面板/视图切换，右侧浮动按钮触发面板）
- **节奏特征**: 缓慢悠闲，无时间压力，无失败状态

---

## 复杂度评估

| 维度 | 评估 | 说明 |
|------|------|------|
| **面板数量** | 6 个主视图 | 庭院场景、节气轮盘、活动面板、风物志、访客记录、庭院布置 |
| **数据量** | 中等 | 24节气 × 3候 = 72候物候 + 24节气活动 + 4类访客 + 节气美食 |
| **交互复杂度** | 中等偏上 | 轮盘旋转、粒子动画、活动小游戏、收集解锁、拖拽放置 |
| **动画要求** | 高 | 季节渐变过渡、飘落粒子、盖章动画、解锁闪光等 |

**整体复杂度**: 中等（单页应用 + 多面板切换 + 丰富动画 + 多种交互模式）

---

## 页面结构总览

> **说明**：单页应用，主容器为庭院场景，多个面板以弹窗/抽屉形式叠加展示。右侧浮动按钮组为面板切换入口。

**页面文件**: `GameApp.tsx`

| 区域/面板 | 层级 | 触发方式 | 说明 |
|-----------|------|---------|------|
| 顶部状态栏 | 固定顶部，始终可见 | - | 当前节气名称、日期、天气、岁时值进度 |
| 庭院场景 | 主背景层（z=0） | 默认展示 | 古风庭院俯视图/侧视图，含菜畦、花圃、池塘、茶亭、竹篱、石径；随季节变色；可点击元素触发互动 |
| 季节性粒子层 | 前景叠加层（z=1） | 自动 | 落花/飘叶/雪花等飘落动画 |
| 右侧浮动按钮组 | 固定右侧（z=10） | - | 风物志、节气轮、访客记录、庭院布置、设置 五个入口 |
| 节气轮盘面板 | 弹窗层（z=20） | 点击"节气轮"按钮 | 圆形24节气轮盘，可旋转选择，中心显示节气信息和三候 |
| 节气活动面板 | 抽屉/弹窗（z=20） | 点击庭院中活动元素 或 节气详情 | 展示当前节气活动卡片，点击弹出互动小游戏 |
| 风物志面板 | 抽屉/弹窗（z=20） | 点击"风物志"按钮 | 72候物候图鉴，分物候/作物/食膳/器物四标签，含收集进度 |
| 访客记录面板 | 抽屉/弹窗（z=20） | 点击"访客记录"按钮 | 访客列表，已遇显示详情，未遇显示"？？？" |
| 庭院布置面板 | 半透明覆盖层（z=15） | 点击"布置"按钮 | 庭院网格+装饰物选择，点击放置/移除，显示美观度评分 |
| 活动互动弹窗 | 最上层（z=30） | 点击活动卡片 | 简单互动小游戏（拖拽/点击/选择等），完成后盖章动画 + 获得岁时值 |

---

## 页面布局建议

- **布局模式**: 全屏沉浸式主场景 + 右侧浮动工具栏 + 弹窗/抽屉面板叠加
  - 桌面端：庭院场景居中铺满，右侧垂直排列浮动按钮，面板从右侧滑出或居中弹出
  - 移动端：庭院场景铺满，底部/右上角菜单按钮，面板全屏或底部弹起
- **视觉重心**: 庭院场景 —— 游戏主体，所有面板围绕场景展开
- **面板承载区**: 节气轮盘（居中圆形弹窗）、活动/风物志/访客记录（右侧抽屉式卷轴面板）、庭院布置（半透明覆盖+底部工具栏）
- **初始态**: 默认显示立春节气庭院，顶部状态栏、右侧浮动按钮、飘落粒子持续运行

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 24节气基础数据（名称/日期/季节/描述/三候/作物/活动/访客） | demo-mock | `src/data/solarTerms.ts` 中内置完整 24 节气常量数组 | ✅ 本身就是 mock，所有数据内置 |
| 节气美食数据 | demo-mock | `src/data/solarTerms.ts` 中内置每节气对应美食（`ISolarTerm.foods`） | ✅ 本身就是 mock |
| 访客基础数据（4类访客详情） | demo-mock | `src/data/visitors.ts` 中内置访客描述与头像配置 | ✅ 本身就是 mock |
| 装饰物数据（石灯/花盆/灯笼等） | demo-mock | `src/data/decorations.ts` 中内置按季节分类的装饰物 | ✅ 本身就是 mock |
| 游戏进度（岁时值/已完成活动/已收集物候/已遇访客/庭院布置） | local-persist | localStorage key=`__game_suishiji_progress`，保存玩家进度对象 | 首次进入为初始空进度（立春起始，0岁时值，无收集） |
| 当前选中节气 | 本地 state | React useState 管理，切换时触发场景过渡动画 | 默认"立春" |
| 当前展开面板 | 本地 state | React useState 管理面板可见性 | 默认全部关闭 |

> **说明**：本游戏为纯前端单机悠闲游戏，所有游戏数据内置为 mock 常量，玩家进度使用 localStorage 持久化。无后端、无 API、无插件需求。

---

## 功能列表

### 顶部状态栏

- **页面目标**: 展示当前游戏状态概览
- **功能点**:
  - **显示当前节气信息**: 节气名称（书法字体）、日期范围、季节标识
  - **显示天气描述**: 根据节气展示对应天气（如"东风解冻""细雨绵绵""大雪纷飞"）
  - **岁时值进度条**: 显示当前累计岁时值和进度（如"岁时值 128 / 240"），用古风卷轴/印章风格进度条
  - **季节主题色适配**: 状态栏背景和文字色随当前季节（春/夏/秋/冬）渐变切换

### 庭院场景（核心主视图）

- **页面目标**: 承载游戏主场景，提供沉浸式节气体验
- **功能点**:
  - **SVG/CSS 绘制古风庭院**: 包含菜畦、花圃、池塘、茶亭、竹篱、石径六大元素，简化俯视图风格
  - **季节主题色切换**: 切换节气时，庭院整体色调（春绿/夏碧/秋金/冬白）通过 CSS transition 平滑渐变过渡
  - **可点击交互元素**: 菜畦点击→种植面板、花圃点击→花卉面板、池塘点击→喂鱼/赏荷、茶亭点击→活动面板入口
  - **季节性元素变化**: 不同节气显示不同作物/花卉（如立春有韭菜、惊蛰有桃花、夏至有荷叶）
  - **飘落粒子动画**: 春季落花、夏季绿叶/萤火、秋季落叶、冬季雪花，使用 CSS keyframes + 随机生成 DOM 元素实现

### 节气轮盘面板

- **页面目标**: 可视化展示24节气，支持节气切换
- **功能点**:
  - **圆形24节气轮盘**: 24个节气按顺时针排列，分为春夏秋冬四段（用不同颜色扇区区分）
  - **轮盘旋转交互**: 支持拖拽旋转或点击节气定位旋转，选中节气转到顶部高亮放大
  - **中心信息展示**: 轮盘中心显示当前选中节气名称、简短描述、日期范围
  - **三候信息展示**: 选中节气后，轮盘下方/内侧展示该节气三候名称（初候/二候/三候）
  - **节气切换**: 点击确认后切换到对应节气庭院，触发场景渐变过渡动画

### 节气活动面板 & 互动小游戏

- **页面目标**: 提供节气专属互动内容，驱动玩家收集和进度
- **功能点**:
  - **活动卡片列表**: 每个节气3个活动卡片，显示活动名称、图标、描述、预计时间
  - **完成状态标记**: 已完成活动显示红色印章式"完成"标记 + 勾选
  - **互动弹窗 - 点击收集类**: 如"踏青寻花""采新茶""踏雪寻梅"，场景中随机出现目标物，点击收集，限时或限次
  - **互动弹窗 - 拖拽匹配类**: 如"咬春饼""包饺子""夏至面"，将食材拖拽到对应位置，完成搭配
  - **互动弹窗 - 点击制作类**: 如"贴春胜""做青团"，按步骤点击制作，有进度反馈
  - **盖章动画**: 完成活动后弹出红色印章旋转盖下的动画，同时获得岁时值飘字提示
  - **岁时值发放**: 每个活动完成后增加固定岁时值（如+10），顶部进度条同步更新

### 风物志/物候图鉴面板

- **页面目标**: 展示收集进度，提供成就反馈
- **功能点**:
  - **分类标签页切换**: 物候、作物、食膳、器物四个分类，标签用古风竖排风格
  - **72候物候图鉴**: 按24节气分组，每候显示候名（如"桃始华"）、描述、收集状态
  - **收集状态可视化**: 已收集显示彩色图标 + 名称，未收集显示灰色剪影 + "？？？"
  - **收集进度条**: 顶部显示总收集进度（如"36 / 72 候"），进度条用卷轴风格
  - **新解锁动画**: 当有新项目解锁时，对应卡片有闪光高亮 + 轻微缩放动效
  - **节气分组折叠**: 可按节气展开/折叠查看该节气下的物候/作物/食膳

### 访客记录面板

- **页面目标**: 展示访客收集，增加故事性和人文趣味
- **功能点**:
  - **访客列表**: 按节气顺序排列，显示访客头像（古风简笔画风格）、名称、身份
  - **已遇/未遇状态**: 已遇到显示详细信息（带来的礼物/故事），未遇到显示"？？？"和剪影头像
  - **访客详情**: 点击访客卡片展开详情，包含身份介绍、来访节气、故事/礼物描述
  - **庭院访客互动**: 当前节气的访客会出现在庭院场景中（茶亭附近），点击触发对话气泡

### 庭院布置/装扮系统

- **页面目标**: 提供个性化庭院装饰玩法
- **功能点**:
  - **可放置网格**: 庭院中显示半透明网格区域，标识可放置位置
  - **装饰物选择栏**: 底部横向滚动装饰物列表，按季节分类（春/夏/秋/冬）
  - **放置/移除交互**: 点击选中装饰物 → 点击网格放置；点击已放置装饰物 → 移除
  - **美观度评分**: 实时显示庭院美观度分数（根据放置装饰物数量和季节匹配度计算）
  - **布置保存**: 布置结果保存到 localStorage，切换节气后保留

### 全局交互与动效

- **面板切换过渡**: 所有面板打开/关闭有平滑滑入滑出 + 淡入淡出动画
- **季节渐变过渡**: 切换节气时，庭院背景色、粒子类型在 1-2 秒内渐变
- **飘落粒子系统**: 基于季节自动生成对应粒子（花瓣/叶片/雪花），随机大小、位置、下落速度
- **古风UI元素**: 所有面板边框用窗棂/卷轴样式，按钮用印章/竹简风格，字体用书法/宋体

---

## 数据共享配置

| 存储键名 | 数据说明 | 使用模块 |
|---------|---------|---------|
| `__game_suishiji_progress` | 玩家全局进度，类型为 `IGameProgress` | 所有面板 |
| 当前节气 state | `currentSolarTerm: string`（节气名称） | 庭院场景、状态栏、活动面板、风物志 |
| 当前面板 state | `activePanel: 'none' \| 'wheel' \| 'activities' \| 'handbook' \| 'visitors' \| 'decorate'` | 全局面板管理 |

```ts
interface IGameProgress {
  /** 当前累计岁时值 */
  yearValue: number;
  /** 已完成的活动ID列表 */
  completedActivities: string[];
  /** 已收集的物候ID列表 */
  collectedPhenology: string[];
  /** 已收集的作物ID列表 */
  collectedCrops: string[];
  /** 已收集的食膳ID列表 */
  collectedFoods: string[];
  /** 已收集的器物ID列表 */
  collectedArtifacts: string[];
  /** 已遇到的访客ID列表 */
  metVisitors: string[];
  /** 庭院装饰物放置记录，key为网格位置ID，value为装饰物ID */
  decorations: Record<string, string>;
  /** 庭院美观度评分 */
  beautyScore: number;
}
```

---

## 技术选型

- **渲染层**: React + Tailwind CSS + SVG 庭院场景
- **理由**: 游戏以静态布局+CSS动画为主，元素数量可控（庭院元素<20个，粒子用CSS动画），无需Canvas或3D。React管理面板状态和数据，SVG绘制古风庭院场景，Tailwind实现响应式和主题色切换。
- **新增依赖**: 无需额外重型依赖，使用 CSS keyframes + SVG 实现所有动画效果
  - `framer-motion`（可选，用于面板切换和盖章等复杂过渡动画，非必须）
  - 若不用 framer-motion，则全部用 CSS transitions/keyframes 实现

---

## 质量基线确认

- [x] 核心功能完整可用（节气切换、活动互动、收集系统、庭院布置均有实际逻辑）
- [x] 有基本的视觉层次（古风主题色+卷轴/印章UI+季节氛围）
- [x] 交互有反馈（按钮点击、盖章动画、岁时值飘字、解锁闪光）
- [x] 边界状态有处理（空收集状态、未完成活动状态、未遇访客状态）
- [x] 进度持久化（localStorage 保存游戏进度）

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 用户给出完整的游戏世界观与视觉描述，但无成品截图或草图，按内容语义自主建立设计系统。
- **核心情绪 / 应用类型**: 古风悠闲经营游戏 / 互动网页原型 —— 缓慢、雅致、随节气轮转的山林隐逸感。
- **独特记忆点**: 节气色随四季在米白宣纸上缓缓晕染，卷轴式面板配以朱砂印章式按钮，飘落粒子随季相变化。

## 2. Art Direction

- **方向名**: 宋韵水墨淡彩
- **Design Style**: 中式古典 + 水墨淡彩 —— 以宋代山水意境为底色，用宣纸、墨线、印章、窗棂构建UI骨架。
- **DNA 参数**: 圆角 subtle (rounded-sm) / 阴影 subtle 且偏暖 / 间距 spacious / 字体方向 宋体衬线+书法点缀 / 装饰手法 窗棂边框、卷轴边、印章朱印、宣纸纹理
- **应用类型**: Game / Interactive Narrative —— 中央庭院场景 + 四周浮动面板 + 全屏节气轮

## 3. Color System

**色彩关系**: 宣纸米白基底 + 淡墨灰文字 + 四季轮替色（春嫩绿/夏青碧/秋金黄/冬银白）+ 朱砂印章红强调
**配色设计理由**: 以宣纸和淡墨建立古籍阅读般的柔和基调，四季色仅用于场景与节气标识，朱砂红作为印章与主行动点形成克制的视觉锚点。
**主色推导**: primary 取朱砂红（宋代印章常用色），用于按钮、印章完成态与关键交互；bg 取宣纸米白；四季色为场景语义色，随节气切换。
**使用比例**: 65% 宣纸中性 / 25% 四季辅助色 / 10% 朱砂 primary；主按钮、印章、当前节气高亮使用 primary，其余状态交由 accent 与四季色。

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(45 35% 96%) | 宣纸米白，带轻微纸纹 |
| card | `--card` | `bg-card` | hsl(42 30% 94%) | 卷轴面板，略深于 bg |
| text | `--foreground` | `text-foreground` | hsl(25 10% 20%) | 淡墨色，正文标题 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(28 8% 45%) | 灰墨色，注释与辅助 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(5 75% 45%) | 朱砂红，印章与主CTA |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(40 50% 96%) | 米白，朱砂上的文字 |
| accent | `--accent` | `bg-accent` | hsl(40 25% 88%) | 浅米色，hover/选中底 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(25 10% 25%) | 深墨色，accent上文字 |
| border | `--border` | `border-border` | hsl(30 15% 75%) | 淡墨线，窗棂与边框 |

**语义色提示**:
- 春季色：嫩绿 hsl(90 45% 72%)，三态 bg hsl(90 50% 92%) / border hsl(90 40% 78%) / text hsl(95 35% 35%)
- 夏季色：青碧 hsl(175 35% 68%)，三态 bg hsl(175 40% 90%) / border hsl(175 30% 75%) / text hsl(180 30% 32%)
- 秋季色：金黄 hsl(40 70% 68%)，三态 bg hsl(42 75% 90%) / border hsl(40 60% 75%) / text hsl(35 55% 30%)
- 冬季色：银白 hsl(210 15% 88%)，三态 bg hsl(210 20% 94%) / border hsl(210 10% 80%) / text hsl(220 10% 40%)
- 成功（收集解锁）：与春季嫩绿对齐；警告与错误本游戏无失败态，不使用。

## 4. 字体与节奏

- **font-display**: Noto Serif SC + ZCOOL XiaoWei —— 宋体为骨，小楷点缀节气名称与印章，体现古籍雅致。
- **font-body**: Noto Serif SC —— 正文用宋体，保持阅读舒适与古风调性。
- **字号**: H1 text-4xl ~ text-5xl（节气名可用书法字体放大）；H2 text-xl ~ text-2xl；body text-base；muted text-sm。
- **圆角**: 小 (rounded-sm) —— 古卷轴与窗棂均为直角或微圆角，避免圆润卡通感。

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导，庭院居中、右侧浮动导航、顶部状态栏、弹层式面板。
- **Page / Section Order**: 单页多面板：①主庭院场景 ②节气轮盘（全屏覆盖弹出） ③活动面板（侧滑/弹出） ④风物志 ⑤访客记录 ⑥庭院布置。
- **Standard Content Zone**: 面板内容 max-w-3xl + mx-auto；庭院场景为全屏画布（full-bleed）。
- **Shell / Frame Alignment**: 庭院场景全宽独立画布；顶部状态栏与右侧浮动按钮为固定 chrome；弹出面板内容受 max-w 约束。
- **Padding & Rhythm**: 面板 `p-6 md:p-8`，卡片间距 `gap-4 md:gap-6`，保持 8px 倍数。
- **Full-bleed Zones**: 庭院场景、节气轮盘为 full-bleed 画布；弹出卷轴面板内含内容区。
- **Local Narrowing**: 风物志详情、访客对话、活动弹窗可收窄至 max-w-xl。
- **Overflow Strategy**: 活动卡片列表、风物志图鉴使用 `overflow-y-auto` 或网格分页。
- **Flexibility Boundary**: 允许移动端调整面板占满屏幕、按钮尺寸放大；不允许改变主色、边框语言与字体方向。

## 6. 视觉与动效

- **装饰**: 窗棂格角、卷轴轴杆、朱砂印章、宣纸纹理、四季飘落粒子
- **阴影/边界**: 轻 —— 用多层淡墨线边框模拟卷轴边缘，阴影极轻偏暖
- **动效**: 精致舒缓 —— 节气切换 1.5s 颜色渐变过渡；印章盖下 0.6s 弹性缩放；粒子缓慢飘落；面板展开用卷轴下拉效果

## 7. 组件原则

- 按钮：default 为描边墨线 + 宣纸底，hover 略深；primary 为朱砂印章形（带印泥质感）。
- 卡片：卷轴式面板，上下有轴杆装饰，四角带窗棂角花。
- 弹窗：从顶部或侧边卷轴展开，背景为半透明宣纸遮罩。
- 加载与空状态：用淡墨线描 + "待君寻访"类文案，延续水墨语言。
- 所有可交互元素必须有 `:focus-visible` 朱砂描边状态。

## 8. Image Direction

- **Image Role**: 庭院场景主视觉 / 节气轮盘背景 / 风物志图鉴图标 / 访客头像
- **Image Art Direction**: 宋代水墨淡彩风格，构图留白充足，远山近庭层次分明；光线柔和漫射，如薄雾清晨；材质为宣纸晕染感，边缘略带毛边；情绪宁静悠远、物我相忘。
- **Image Prompt Keywords**: 宋代山水画, 水墨淡彩, 宣纸纹理, 留白构图, 古典庭院, 四季风物, 朱砂印章, 薄雾朦胧, 工笔兼写意, 低饱和度
- **Image Avoidance**: 卡通Q版画风, 浓艳高饱和色彩, 3D渲染感, 日式动漫风格, 照片合成感, 现代建筑元素, 电商素材图库感

## 9. Anti-patterns

- **Split personality**: 不同面板使用不同字体或圆角；全站统一宋体+微圆角+墨线边框。
- **Phantom tokens**: 编造不存在的古风化 CSS 变量名；只使用已定义的 9 个基础 token + 四季语义色。
- **Default SaaS drift**: 回到默认蓝色按钮与卡片阴影；用朱砂印章与卷轴语言替代一切通用组件。
- **Invisible interaction**: 只做 hover 不做 focus-visible；所有可交互元素必须有朱砂色键盘焦点环。
- **Mono-hue tyranny**: 朱砂红铺满按钮、边框、链接、图标；primary 只用于印章与关键行动，其余用墨线与四季色分担。
- **Status color drift**: 四季色饱和度过高刺眼；所有四季色饱和度控制在 35%-50% 之间，与 primary 75% 拉开权重差。